apiVersion: v1
kind: Service

metadata:
  labels:
    service: frontend-srv
    app: canary-frontend
  name: frontend-srv

spec:
  type: NodePort
  selector:
    app: canary-frontend
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80